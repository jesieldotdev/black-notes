{{>_novanota}}


<!-- Card note -->
<div  class="row row-cols-1 row-cols-md-3 g-4 mt-4 shadow-lg rounded border-radius: 30px;">
  {{#each notes}}
  <div class="col">
    <div id="card-note" style=background-color: {{cor}} name="id" class="card bg-dark text-light border-0">
      
      <a href="/note/{{_id}}"><img style="border-radius: 25px;" id="capa" src="{{img_link}}" class="card-img-top" alt="..."/></a>
      <div class="card-body">
        <h4 id="note-title" class="card-title">{{title}}</h4>
        <p id="description" class="card-text">
          {{note}}
        </p>
        <small>{{date}}</small>
      </div>



    <!-- Buttons Group -->
      <div class=''>

        <!-- Botão Compartilhar Nota -->
        <!-- {{!-- <form method="POST" action="/note" class="mx-auto "> --}}
          <input type="hidden" name="id" value='{{_id}}'>
           <a href="note/{{_id}}"><button class="btn btn-dark btn-floating mt-2 " value="" name=""><i class="fas fa-share"></i></button></a>
        {{!-- </form> --}} -->

        <!-- Botão Excluir Nota -->
        <form method="POST" action="/deletar" class=" mx-auto">
          <input type="hidden" name="id" value='{{_id}}'>
           <button type="submit" class="btn btn-dark btn-floating mt-2 " value="" name=""><i class="fas fa-trash"></i></button>
        </form>

      {{>_dropdownmenu}}
      </div>


  </div>
    </div>

  {{else}}
  {{/each}}

</div>


           

           